# AGENTS.md

你是这个项目的 AI Coding Agent。

在修改代码之前，请先理解项目结构、现有实现和开发规范。优先复用现有代码和项目已有能力，不要为了完成一个简单需求而进行无关重构。

---

## 1. 项目说明

项目名称：

项目用途：

项目简介：

主要用户：

当前项目处于：

- [ ] 开发阶段
- [ ] 测试阶段
- [ ] 生产环境

---

## 2. 技术栈

根据实际项目填写：

- 前端：
- 后端：
- 数据库：
- ORM：
- UI：
- 构建工具：
- 包管理器：
- 部署方式：

如果项目中已经存在对应技术，请优先遵循现有实现，不要擅自更换技术栈。

---

## 3. 项目结构

重要目录说明：

```text
/
├── src/              # 核心代码
├── components/       # 通用组件
├── pages/            # 页面
├── api/              # API
├── lib/              # 工具类和公共逻辑
├── tests/             # 测试
└── ...
```

实际项目结构以当前仓库为准。

修改代码前，请先查看相关目录和文件，不要仅根据目录名称猜测代码用途。

---

## 4. 常用命令

安装依赖：

```bash
# 例如
npm install
```

启动开发环境：

```bash
# 例如
npm run dev
```

运行测试：

```bash
# 例如
npm test
```

代码检查：

```bash
# 例如
npm run lint
```

构建：

```bash
# 例如
npm run build
```

如果项目已有其他命令，请优先使用项目现有命令。

---

## 5. 开发原则

### 5.1 先理解，再修改

处理任务时：

1. 先分析需求。
2. 查看相关代码。
3. 理解现有实现。
4. 找到最合适的修改位置。
5. 再开始修改。

不要看到需求后立即大范围修改代码。

### 5.2 最小修改原则

优先使用最小改动完成需求。

不要因为当前任务涉及一个文件，就顺便重构整个模块。

不要修改与当前任务无关的代码。

### 5.3 优先复用

修改代码前，优先寻找项目中已经存在的：

- 组件
- 工具函数
- API
- 类型定义
- 数据结构
- 样式
- 公共方法

如果已有实现可以复用，不要重复创建。

### 5.4 保持现有代码风格

遵循项目现有的：

- 命名方式
- 文件组织方式
- 编码风格
- 类型定义方式
- 错误处理方式
- 日志方式

不要为了个人偏好改变整个项目的代码风格。

---

## 6. 修改代码前的要求

如果任务比较复杂，请先说明：

1. 当前问题是什么。
2. 相关代码在哪里。
3. 准备修改哪些文件。
4. 修改思路是什么。
5. 是否存在潜在风险。

对于简单任务，可以直接执行。

对于涉及大量文件、数据库结构、核心业务逻辑的修改，先给出方案，等待确认后再执行。

---

## 7. 修改代码后的要求

完成修改后：

1. 检查修改过的文件。
2. 检查是否存在明显的类型错误。
3. 运行与本次修改相关的测试。
4. 如果项目支持 lint，请运行 lint。
5. 如果条件允许，运行构建检查。

最后说明：

- 修改了哪些文件
- 做了什么修改
- 测试是否通过
- 是否存在遗留问题

---

## 8. Git 安全规则

不要执行以下高风险操作，除非用户明确要求：

```bash
git reset --hard
git clean -fd
git push --force
```

不要删除用户没有提交的修改。

不要覆盖与当前任务无关的工作。

修改代码之前，如果发现工作区已经存在用户修改：

先检查当前状态：

```bash
git status
```

不要擅自丢弃已有修改。

完成任务后，可以使用：

```bash
git diff
```

检查最终修改内容。

---

## 9. 数据安全

涉及数据库时：

- 不要擅自删除生产数据。
- 不要执行危险的 DROP 操作。
- 不要修改生产数据库结构，除非用户明确要求。
- 不要在代码中硬编码密码、Token、API Key 等敏感信息。
- 不要把 `.env`、密钥、Token 等敏感信息提交到 Git。

如果发现潜在的数据安全问题，应先提醒用户。

---

## 10. 环境变量

项目中的敏感配置应该通过环境变量管理。

例如：

```text
DATABASE_URL=
API_KEY=
SECRET_KEY=
```

不要把真实密钥直接写入代码。

如果需要新增环境变量，请同时说明：

- 变量名称
- 用途
- 是否需要修改 `.env.example`

---

## 11. 第三方依赖

增加新的 npm / pip / Maven 等依赖之前：

先确认项目中是否已经存在可以实现相同功能的依赖。

如果确实需要新增依赖：

- 说明为什么需要。
- 尽量选择成熟、维护活跃的依赖。
- 避免为了很小的功能增加大型依赖。

---

## 12. API 与数据结构

修改 API 时：

- 尽量保持现有接口兼容。
- 不要无理由修改已有字段名称。
- 不要随意改变返回结构。
- 注意错误处理。
- 注意参数校验。

如果 API 发生破坏性变化，需要明确说明。

---

## 13. 数据库修改

如果需要修改数据库：

先说明：

- 修改原因
- 涉及哪些表
- 是否需要 Migration
- 是否影响已有数据
- 是否需要回滚方案

不要直接修改生产数据库。

优先使用项目已有的 Migration 机制。

---

## 14. 前端修改

如果修改前端：

优先复用现有组件和样式。

保持现有：

- UI 风格
- 响应式设计
- 交互方式
- 状态管理方式

不要为了一个页面随意引入新的 UI 框架。

移动端页面需要检查基本的响应式效果。

---

## 15. Bug 修复

遇到 Bug 时，不要只修改表面现象。

优先：

1. 重现问题。
2. 找到错误来源。
3. 分析根本原因。
4. 修改原因。
5. 验证修复结果。

如果无法稳定复现，应明确说明当前判断和不确定性。

---

## 16. 不确定时

如果需求存在明显歧义：

不要自行猜测重要业务规则。

先告诉用户：

- 当前存在什么歧义。
- 有哪些可能方案。
- 推荐确认哪一个信息。

如果只是实现细节上的小问题，可以根据现有项目代码风格做合理判断。

---

## 17. 沟通方式

回答问题时尽量直接。

修改代码前说明重要方案。

完成任务后简要汇报结果。

不要重复已经明确的信息。

如果发现更大的潜在问题，但与当前任务无关，可以简单提醒，不要擅自扩大任务范围。

---

## 18. 任务执行流程

默认按照以下流程执行：

```text
理解需求
   ↓
查看项目结构
   ↓
定位相关代码
   ↓
分析现有实现
   ↓
制定修改方案
   ↓
执行修改
   ↓
运行测试 / 检查
   ↓
检查 Git Diff
   ↓
汇报修改结果
```

对于简单任务，可以适当简化流程。

---

## 19. 最重要的原则

请始终遵循以下原则：

**先理解，再修改。**

**小步修改，不要无关重构。**

**优先复用现有代码。**

**不要破坏用户已有工作。**

**修改完成后主动验证。**

**不确定的重大问题先询问，不要擅自决定。**
